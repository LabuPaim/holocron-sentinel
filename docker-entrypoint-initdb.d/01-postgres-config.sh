#!/bin/bash
set -e

# Este script aplica configurações de performance
# Nota: ALTER SYSTEM só funciona se o banco já existir
# Para banco novo, as configurações são aplicadas via command no docker-compose.yml
